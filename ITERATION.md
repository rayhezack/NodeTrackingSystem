# Pollo AI 埋点管理系统迭代文档

**核心结论：当前版本已经从“单事件表单工具”演进为“需求单粒度的埋点项目管理系统”，重点解决了多事件需求、正式库复用、权限协作、Base 同步和性能体验问题。**

## 当前线上版本

- **最新提交：** `3298a0e`
- **最新发布：** `7667528632269917456`
- **线上地址：** [https://bcn0tgplxp2e.aiforce.cloud/app/app_17apvbcusvs](https://bcn0tgplxp2e.aiforce.cloud/app/app_17apvbcusvs)
- **版本主题：** 提交评审从埋点事件粒度升级为需求单粒度。

## 版本能力总览

| 能力域 | 当前状态 | 业务价值 |
|---|---|---|
| 需求创建 | 已支持 | 普通业务方可提交埋点需求并指定负责人 |
| 多事件需求 | 已支持 | 一个需求可包含多个 evt_id，符合真实业务需求 |
| 参数设计 | 已支持 | 支持参数增删改、批量删除、枚举维护、版本继承 |
| 复用正式事件 | 已支持 | 支持在正式库事件基础上做增量修订 |
| 工作流推进 | 已支持 | 覆盖提需、设计、评审、开发、验收、上线、归档 |
| 正式库同步 | 已支持 | 归档后事件和参数自动进入正式查询库 |
| 权限模型 | 已支持 | 管理员 + 普通用户 + 项目角色协作 |
| 性能优化 | 已支持 | 工作台合并加载、详情切换缓存、参数局部更新 |

## 近期迭代脉络

### 需求单粒度工作流

**核心变化：系统从 evt_id 粒度逐步调整为需求单粒度，解决多事件需求在工作台和流程推进中的割裂问题。**

- `e9cc64a`：支持一个需求下新增多个埋点事件。
- `ef47d26`：工作台按需求单聚合展示，不再把同一需求下的多个 evt_id 拆成多条需求。
- `bc2d897`：需求名称同步到同需求下的所有事件，方便 Base 查询。
- `2ec108c`：同需求事件切换使用缓存，不再每次切换都重载详情页。
- `3298a0e`：埋点设计提交评审按需求单粒度推进，同一需求下所有事件一起进入评审。

### 埋点设计与参数维护

**核心变化：参数设计从简单录入升级为可维护、可复用、可交付的设计工作台。**

- `d88cf90`：将参数设计嵌入埋点设计环节。
- `3b57b4f`：参数从“作废”语义调整为设计阶段的正常删除语义。
- `8762fa3`：参数版本默认继承埋点事件版本，减少重复填写。
- `d4be678`：优化参数设计流程，支持更高效的参数填写和保存。
- `1278ab1`：复用正式事件时只导入选中的正式参数，避免全量复制造成冗余。
- `f360511`：新增研发交付视图，研发可以直接查看事件和全部参数明细。

### 复用现有事件与并行修订

**核心变化：支持多个需求并行修订同一个正式事件，最终归档时增量合并到正式库。**

- `c30ac9f`：支持在埋点设计环节复用已有正式事件。
- `1278ab1`：只导入本次需要修改的参数，避免复制全部正式参数。
- `e488ea1`：支持同名 evt_id 在不同需求之间并行修订，通过 `需求ID + evt_id` 隔离。
- `3b51809`：新增“仅校验（不修改正式库）”模式，适配只提醒开发校验、不做正式库变更的需求。

### 正式查询库与归档同步

**核心变化：归档不再只是工作流状态变化，而是会把事件和参数同步到可查询的正式库。**

- `323131e`：成功上线/归档后的事件自动同步到正式查询库。
- `0df6983`：归档时同步设计参数到正式参数库。
- `4cd0e1c`、`8ff5aed`：补强正式查询库字段和参数入口。
- `3b51809`：仅校验事件归档时不修改正式查询库。

### 权限与协作模型

**核心变化：权限从固定职能角色配置，调整为“管理员 + 普通用户 + 需求内项目角色”。**

- `94c203b`：简化权限配置逻辑，突出管理员和普通用户。
- `d88adb8`：项目参与人和角色待办进入工作流。
- `dcc1aa0`：研发负责人被指定后，拥有从需求录入到埋点开发的阅读和编辑权限；待办按需求角色展示。
- `9bda1cd`：修复工作流表单中负责人展示为未知用户或数字 ID 的问题。

### Base 契约与字段类型治理

**核心变化：持续收敛前端表单、后端写入和 Base 字段类型之间的契约，减少必填和类型错误。**

- `351e1d4`：修复创建需求阶段的必填字段和负责人展示问题。
- `bb8f90f`：加固 Base 字段契约和工作流状态跳转。
- `d35d2a4`：修复参数写入 Base 双向链接字段类型。
- `b1594c9`：规范 UI 图字段写入，避免附件对象写入文本字段。
- `c2dc04a`：去重参数适用端选项，避免“App 通用”和“仅 App”语义重复。

### 性能与体验优化

**核心变化：减少高频 Base 读取和整页刷新，提升工作台和详情页的连续编辑效率。**

- `46c96cd`：优化工作台加载性能，并拆分前端大 chunk。
- `d4be678`：参数新增、编辑、删除后本地更新，不再整页刷新。
- `2ec108c`：同需求事件详情一次读取，事件切换走本地缓存；新增/删除/完成节点时才刷新详情。
- `398952d`、`675726f`、`977c97d`：完善 UI 图预览能力，兼容存储路径、文件名和预览链接。

## 当前版本详细说明：`3298a0e`

**核心结论：埋点设计阶段的“提交评审”已从当前事件动作升级为需求单动作。**

- **变更前：** 一个需求下有多个 evt_id 时，点击某个事件的“提交评审”只会把当前事件置为评审中，其它事件仍停留在埋点设计。
- **变更后：** 点击任意事件的“提交评审”后，同一 `需求ID` 下所有埋点事件都会进入埋点评审。
- **保护逻辑：** 当前事件会保存当前表单中的设计字段；其它事件只更新 `评审状态=评审中`，不会被覆盖 evt_id、事件定义、触发时机、UI 图等事件级字段。
- **验证结果：** 已补充单测覆盖需求粒度评审提交逻辑，并通过 lint、type check、server build、client build。

## 当前产品边界

- **需求粒度：** 工作台、待办、提交评审应按需求单聚合。
- **事件粒度：** 事件定义、触发时机、UI 图、参数明细仍按 evt_id 单独维护。
- **参数粒度：** 参数新增、删除、枚举追加按参数主键和 evt_id 隔离。
- **正式库同步：** 仅在归档/上线链路中同步正式库；设计阶段只是维护草稿或修订内容。

## 后续建议

**优先级最高的是继续强化“需求单聚合 + 事件级设计”的边界，避免用户在多事件需求中产生状态歧义。**

- **补充需求级概览：** 在详情页顶部增加同需求全部事件的设计完成度、参数数量和评审状态汇总。
- **增加变更 diff：** 复用正式事件时展示“本次新增参数 / 修改枚举 / 仅校验”的差异摘要。
- **完善审计日志：** 记录谁在何时推进了节点、修改了负责人、修改了参数或枚举。
- **优化 Base 回写监控：** 对归档同步失败、参数合并失败、字段类型错误做更明确的错误提示。
- **沉淀操作手册：** 给产品、数据、研发分别写简版使用说明，降低跨职能使用成本。
