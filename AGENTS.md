# UI 设计指南

> **设计类型**: App 设计（应用架构设计）
> **确认检查**: 本指南适用于可交互的应用/网站/工具。

> ℹ️ Section 1 为设计意图与决策上下文。Code agent 实现时以 Section 2 及之后的具体参数为准。

## 1. Design Archetype (设计原型)

### 1.1 内容理解

- **目标用户**: 数据部门 DS/研发，高频操作埋点全流程，追求效率与准确性
- **核心目的**: 高效管理埋点生命周期，快速定位待办与状态流转
- **情绪基调**: 专注、掌控感 / 避免焦虑、信息过载

### 1.2 设计方向

- **Design Style**: Grid 网格 — 内部运营系统需高密度信息展示，sharp 圆角+紧凑间距强化数据可读性
- **Application Type**: Admin/SaaS（内部工具） — 决定高视口利用率布局策略
- **Aesthetic Direction**: 克制理性的工程美学，飞书蓝为唯一操作锚点，语义色仅用于状态区分

## 2. Color System (色彩系统)

> 基于飞书 Base 数据源与企业内部运营场景推导，确保与飞书生态视觉一致且降低认知负担。

**色彩关系**: 飞书蓝主色 + 中性灰底 + 深墨文字 + 低饱和语义色
**配色设计理由**: 延续飞书 Base 视觉基因，减少跨工具切换认知成本；中性底色衬托数据密度
**主色推导**: Primary 取自飞书品牌蓝，用于所有可编辑操作、流程激活态、链接跳转
**使用比例**: 60% 中性背景 / 30% 卡片白 / 10% 飞书蓝+语义色；Primary 仅出现在按钮、激活节点、链接

### 2.1 主题颜色

> **Color Token 语义速查**:
> - `primary` → 主行动：保存按钮、流程激活节点、链接
> - `accent` → 状态反馈：表格行 hover、下拉项 focus、骨架屏
> - `muted` → 静态非交互：只读字段背景、禁用态、占位符

| Token                | HSL 值             | 说明                         |
| -------------------- | ------------------ | ---------------------------- |
| `background`         | hsl(220, 14%, 96%) | 页面底色，冷灰营造专业感     |
| `card`               | hsl(0, 0%, 100%)   | 卡片/容器背景                |
| `foreground`         | hsl(222, 47%, 11%) | 主文字，深墨蓝              |
| `muted-foreground`   | hsl(220, 9%, 46%)  | 次要文字/标签               |
| `primary`            | hsl(217, 91%, 60%) | 飞书蓝，主操作色            |
| `primary-foreground` | hsl(0, 0%, 100%)   | 主操作文字                  |
| `accent`             | hsl(220, 14%, 92%) | 交互反馈背景                |
| `accent-foreground`  | hsl(222, 47%, 11%) | accent 上的文字             |
| `border`             | hsl(220, 13%, 91%) | 边框/分隔线                 |

### 2.2 导航区配色

- **基调关系**: 复用主配色，顶部条背景 `card`，底部 `border` 分隔
- **关键状态**: 激活态 `primary` 文字+下划线；Hover `accent` 背景；对比度 ≥ 4.5:1
- **边界与背景**: 非透明 `card` 背景，底部 1px `border` 分隔内容区

### 2.3 语义颜色

| 用途      | HSL 值              | 衍生规则                     |
| --------- | ------------------- | ---------------------------- |
| 成功/上线 | hsl(160, 84%, 39%)  | 背景 hsl(160, 84%, 96%)     |
| 警告/评审 | hsl(38, 92%, 50%)   | 背景 hsl(38, 92%, 96%)      |
| 错误/废弃 | hsl(0, 84%, 60%)    | 背景 hsl(0, 84%, 96%)       |
| 进行中    | hsl(217, 91%, 60%)  | 背景 hsl(217, 91%, 96%)     |

## 3. Typography (字体排版)

- **Heading**: Inter + system-ui, -apple-system, sans-serif
- **Body**: Inter + system-ui, -apple-system, sans-serif
- **字体策略**: Inter 兼顾中英文数字对齐；回退系统字体保障内网环境兼容；正文 text-sm/table 用 text-xs 适配高密度

## 4. Layout Strategy (布局策略)

- **导航意图**: 全局顶部导航栏（埋点工作台 / 正式查询库），至多一套；非透明背景；移动端折叠为汉堡菜单
- **页面架构**: 高视口利用率多区布局，内容区独立滚动；max-w-[1400px] 居中约束
- **响应式**: 桌面端左右分栏详情页；移动端堆叠为单列+底部阶段导航

## 5. Visual Language (视觉语言)

- **形态参数**: 圆角 `rounded-sm (2px)` · 阴影 `shadow-none` · 间距基调 `compact`
- **识别签名**: 4px 以下锐利圆角；流程节点用实心圆+连接线；表格行高 ≤ 40px
- **装饰策略**: 无装饰图形；仅用 1px 边框和语义色块建立层级
- **动效原则**: 即时反馈，150ms ease-out；流程节点切换平滑滚动
- **可及性**: 对比度 ≥ 4.5:1；状态标签同时用颜色+文字双重编码

## 6. Component Principles (组件原则)

- **状态完整性**: Button/Input/Table Row 覆盖 Default/Hover/Focus/Disabled；Focus 用 `ring-2 ring-primary/50`
- **层级清晰**: Primary 按钮填充色；Secondary 描边；Ghost 仅文字+hover 背景
- **一致性**: 表格/表单/流程条统一 sharp 圆角；状态标签用 capsule 但保持 rounded-sm

## 7. Image Direction (图片与视觉资产)

- **Image Role**: 无强制图片需求，优先通过排版、色彩和局部图形建立视觉记忆点
- **Image Art Direction**: 无
- **Image Prompt Keywords**: 无
- **Image Avoidance**: 禁止渐变插画、科技感装饰图、人物素材、抽象背景

## 8. 应避免 (Anti-patterns)

- ❌ 使用大圆角(pill)/毛玻璃/渐变背景破坏工程工具的专业感
- ❌ 统计卡片嵌套卡片或使用深色大屏风格，违背白底中性灰约束
- ❌ 流程条使用胶囊堆叠或复杂图标，应简化为圆点+连线+文字