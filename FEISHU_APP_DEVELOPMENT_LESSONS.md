---
type: retrospective
created: 2026-08-01
updated: 2026-08-05
status: maintained
project: Pollo AI 埋点项目管理系统
source: "/Users/ray/Desktop/Codex Projects/Pollo Projects/SQL查询/app_tracking_management/FEISHU_APP_DEVELOPMENT_LESSONS.md"
tags:
  - retrospective
  - codex
  - feishu
  - miaoda
  - app-development
  - notification
up:
  - "[[复盘库索引]]"
related:
  - "[[知识库结构说明]]"
aliases:
  - FEISHU_APP_DEVELOPMENT_LESSONS
  - 飞书开发经验复盘
---

# 飞书与妙搭小程序开发经验复盘

> [!abstract] 结论
> 这轮迭代反复出问题，主要因为开发前没有把 Base 字段、业务粒度、人员身份、机器人权限和发布验证整理成一套契约。最典型的误判是把不同应用生成的 `open_id` 当成通用人员 ID。代码测试全部通过，真实机器人仍然发送失败，说明外部权限和可用范围必须进入发布门禁。

本文记录 Pollo AI 埋点项目管理系统在 2026 年 8 月的开发与修复过程。内容围绕问题为什么连续出现，以及下次如何在开发前挡住同类错误。

## 项目结果

本轮完成了以下修复：

- 需求列表不再展示大量未知负责人，孙文和刘桥的头像可以正常补充加载。
- 新需求的数据负责人默认填写孙文，埋点校验人默认填写孙文和刘桥，需求提出人使用当前创建者。
- 归档数据回传补齐数据负责人、校验人和稳定归档时间。
- 归档完成后返回工作台，不继续停在已经归档的需求页。
- 新增需求和删除需求单按钮恢复正确的语义颜色。
- 机器人通知改为通过邮箱查询当前机器人可用的飞书 `user_id`，不再直接使用 `open_id`。

最终代码提交为 `202c2147d8c865d75abb0ed01462a30e2885ee7c`。发布任务 `7670471294572039404` 返回 `status=finished`，线上提交与目标提交一致。14 个测试套件、124 个测试、Lint、前后端类型检查和生产构建均通过。

> [!warning] 验证边界
> 代码与权限配置已经发布，但完整闭环仍需要一次真实流程通知作为证据。Mock 测试可以证明分支逻辑，不能证明飞书开放平台的权限、通讯录范围和机器人可用范围已经生效。

## 事故链路

| 阶段 | 线上表现 | 当时的误判 | 最终处理 |
|---|---|---|---|
| Base 归档 | 查询库中的数据负责人、校验人、稳定归档时间为空 | 归档成功等于所有字段都已同步 | 补齐归档字段映射，并对人员字段做统一序列化 |
| 需求列表 | 大量未知负责人，默认人员只有名字没有头像 | 有姓名就不需要继续查询用户资料 | 数字妙搭 ID 缺头像时继续请求资料，姓名只作为加载兜底 |
| 新建需求 | 角色重复，默认人员与业务要求不一致 | 前后端各自维护默认值不会出问题 | 默认人员集中到共享配置，前后端使用同一来源 |
| 节点通知 | `open_id cross app` | `open_id` 可以跨应用复用 | 使用邮箱查询机器人自己的租户级 `user_id` |
| 权限修复 | 已开通邮箱查询权限，仍返回 `99991672` | 接口权限等于返回字段权限 | 同时开通查询接口权限和返回 `user_id` 的字段权限 |
| 发布验证 | 单元测试通过，生产通知仍失败 | Mock 成功等于线上链路成功 | 将机器人权限、数据范围、应用发布和真实消息纳入冒烟测试 |

问题按顺序暴露。第一次修复解决了数据格式，随后出现身份作用域错误；身份作用域改对后，又遇到字段权限；权限开通后，还要发布机器人应用并刷新妙搭运行时。每次只处理眼前报错，才导致修复轮次不断增加。

## 最主要的错误：混用人员身份

飞书和妙搭同时存在多套人员标识。它们看起来都像用户 ID，但作用域不同。

| 标识 | 作用域 | 本项目中的用途 | 是否可直接给机器人发消息 |
|---|---|---|---|
| 妙搭数字 `user_id` | 妙搭运行时或人员组件 | 表单选择、页面展示、Base 人员字段 | 不可以 |
| 飞书 `open_id` | 单个飞书应用 | 标识该应用下的用户 | 只有来源应用与机器人应用相同时才可以，本项目不直接使用 |
| 飞书 `user_id` | 企业租户 | 机器人消息接收人 ID | 可以 |
| `email` | 企业通讯录 | 跨系统查找用户的稳定输入 | 先查询 `user_id`，再发送 |
| `enterprise_email` | 用户受雇信息字段 | 展示或组织信息 | 不能假设查询接口一定接受，应保留实际 `email` |

本项目使用 `feishu-sunwen-cli` 查询深圳九瓴科技有限公司通讯录，用它确认姓名和邮箱。孙文对应 `ray@mail.pollo.ai`，刘桥对应 `joe@mail.pollo.ai`。CLI 返回的 `open_id` 属于 CLI 自己的应用，不能交给埋点管理系统的机器人发送消息。

> [!bug] 曾经采用的错误方案
> 先拿 `feishu-sunwen-cli` 返回的 `open_id`，再让另一个机器人应用发送消息。飞书返回 `code=99992361, msg=open_id cross app`。这个错误不是人员不存在，而是 ID 的来源应用不对。

### 正确的通知链路

```mermaid
flowchart LR
    A["需求单人员快照"] --> B["标准化姓名和 email"]
    B --> C["机器人 tenant_access_token"]
    C --> D["batch_get_id 查询 user_id"]
    D --> E["租户级 user_id"]
    E --> F["im/v1/messages 发送消息"]
```

请求顺序如下：

1. 创建需求时保存参与人的 `name`、`email` 和原始人员字段 ID。
2. 节点完成后，从需求快照中收集所有参与人并按邮箱去重。
3. 机器人使用自己的 `tenant_access_token` 调用 `POST /contact/v3/users/batch_get_id?user_id_type=user_id`。
4. 请求体传入 `emails`，响应中读取租户级 `user_id`。
5. 调用消息接口，设置 `receive_id_type=user_id`。
6. 每个接收人独立发送和记录结果，一个人失败不能阻断其他人。

有邮箱的用户不应回退到 `open_id`。系统无法可靠判断一个外部 `open_id` 是否属于当前机器人应用，回退只会让同一个跨应用错误再次出现。

## 机器人权限不是一个开关

这次线上报错暴露了两个名字相近、用途不同的权限。

| 权限 | 用途 | 缺失时的表现 |
|---|---|---|
| `contact:user.id:readonly` | 允许通过邮箱或手机号调用用户 ID 查询接口 | 无法调用 `batch_get_id` |
| `contact:user.employee_id:readonly` | 允许接口返回 `user_id/employee_id` 字段 | `99991672 Access denied` |
| `im:message:send_as_bot` | 允许应用机器人发送消息 | 消息接口拒绝发送 |

权限页面显示 `Added` 仍然不代表线上已经生效。机器人通知还依赖下面四层配置：

1. API 权限已经添加。
2. 通讯录数据范围包含目标用户。
3. 机器人应用可用范围包含目标用户或所在部门。
4. 新应用版本已经发布并通过管理员审批。

机器人使用应用身份，不需要让孙文重新登录授权。权限发布后，如果妙搭服务仍持有旧运行时状态，可以重新发布妙搭应用，让新进程重新获取凭证。

## 其他工程错误

### Base 字段契约没有前置冻结

Base 不是普通关系型数据库。日期、人员、附件、单选、多选、双向链接和公式字段都有自己的写入格式。早期开发根据前端表单猜测字段约束，导致下游字段在提需阶段被误判为必填，也导致归档时只写了事件主体，没有完整写回负责人和归档时间。

后续做法：

- 开发前导出每张表的字段名、类型、必填性、只读属性和示例值。
- 按流程节点列出允许写入的字段，不让下游字段反向阻塞上游创建。
- 所有 Base 写入经过统一适配层，人员、日期、附件和链接字段禁止在业务代码里临时拼装。
- 保存、节点完成和归档之后做回读校验，不能只相信写接口返回成功。

### 业务粒度混乱

需求、事件和参数是三个层级。工作台、待办、权限、通知和状态推进应该以需求 ID 为主键；事件设计使用 `evt_id`；参数合并使用参数 key。早期逻辑让 `evt_id` 承担了需求主键的职责，多个事件、并行修订和需求级状态因此互相影响。

现在采用下面的边界：

| 粒度 | 管理对象 |
|---|---|
| 需求单 | 工作台、待办、参与人、流程状态、通知 |
| 事件 | `evt_id`、事件名、定义、触发时机、适用端、UI 图 |
| 参数 | 参数 key、类型、必传规则、枚举值、版本 |
| 归档合并 | `需求ID + evt_id + 参数key` |

同一个正式事件允许被多个需求同时修订。设计草稿必须用需求 ID 隔离，归档时增量合并，不能覆盖正式库中的历史参数和枚举。

### 草稿、废弃和正式库复用

这三个动作曾被混在一起处理，结果是草稿删除污染正式库，复用事件又复制出大量无关参数。后续实现按业务语义区分：

| 动作 | 正确行为 |
|---|---|
| 设计阶段删除 | 删除当前需求下的草稿，不写入正式废弃表 |
| 正式事件废弃 | 记录废弃状态和下线信息，从正式查询视图中隔离 |
| 复用正式事件 | 只导入本次需要修改的参数、枚举或事件字段 |
| 参数枚举更新 | 追加新增值并去重，保留历史枚举 |
| 仅校验需求 | 完成校验和流程记录，不修改正式库内容 |

需求链接不能充当主键。链接可能变化，也可能为空。系统必须生成稳定且唯一的需求 ID，用它聚合同一需求下的事件，并隔离不同需求对同一个 `evt_id` 的并行修订。

### 权限和性能边界

全局权限只区分管理员和普通用户。数据负责人、研发负责人、需求提出人和埋点校验人属于需求单角色，权限应跟随具体需求。被指定的参与人需要读取前序内容，并在负责节点获得编辑权限。

详情页也不应在每次切换事件、保存参数后重新加载整张表。一次读取同需求数据，保存后局部更新；只有关键写入需要回读 Base。这样既减少接口调用，也避免页面状态被整页刷新打断。

### 人员默认值分散

新增需求的默认参与人曾分别写在前端表单和后端兜底逻辑里，导致数据负责人、校验人和需求提出人不一致。需求录入者与需求提出者又是同一人，角色重复让通知范围更难判断。

现在把默认人员放在共享配置中：

- 需求提出人是当前创建者。
- 数据负责人默认孙文。
- 埋点校验人默认孙文和刘桥。
- 研发负责人由需求方选择。
- 需求录入者字段不再单独维护。

### 头像加载条件写错

列表组件原先把有姓名视为资料完整，因此孙文和刘桥只有名字，没有头像。正确条件应该是数字妙搭 ID 且缺头像时继续拉取资料。请求期间保留已有姓名，头像失败时使用首字作为兜底。

### UI 样式只看组件，没有检查最终 CSS

新增需求和删除需求单按钮的文字颜色被 Tailwind 与组件变体的优先级覆盖。JSX 中的变体名称正确，但浏览器计算样式已经变成黑色。修复时增加组件级语义颜色，并用 Playwright 检查最终 `color` 和截图，而不是只读 className。

### 发布完成后没有立即验证目标提交

妙搭发布读取远端 `sprint/default`，不会读取本地工作区。正确顺序是提交、推送、创建 release、轮询 release，再比对 `commit_id`。`release-create` 返回 `publishing` 只表示任务已经启动，不代表上线完成。

## 测试为什么没有提前发现问题

这次有一个直接证据：124 个测试全部通过，生产通知仍然失败。

单元测试使用 Mock 响应，可以验证下面这些逻辑：

- 是否先查 `user_id` 再发消息。
- 多人通知是否逐个隔离。
- 权限错误是否返回可操作的提示。
- 缺邮箱时是否停止使用跨应用 `open_id`。

它验证不了真实环境中的权限审批状态、通讯录数据范围、机器人可用范围和邮箱是否能被当前应用查询。当时把 Mock 通过等同于彻底修复，这是一次明确的判断失误。

发布门禁应该分成三层：

| 层级 | 验证内容 | 通过标准 |
|---|---|---|
| 代码 | 单元测试、Lint、类型检查、生产构建 | 命令全部成功 |
| 集成 | 真实 Base 创建、保存、归档和回读 | 字段值与人员信息完整 |
| 线上 | 真实测试需求推进、机器人消息、页面跳转 | 消息送达且 release 提交正确 |

## 通知可靠性仍有欠账

当前流程会先完成节点状态更新，再发送机器人消息。因此可能出现节点已经完成，但通知失败的情况。这个选择避免了飞书故障阻塞主业务，但也带来两个问题：失败消息不会自动补发，用户也没有独立的重新发送入口。

更稳妥的实现是 Outbox：

1. 节点状态和通知任务一起落库。
2. 后台任务异步发送通知。
3. 失败后按退避策略重试，并保存每个接收人的错误。
4. 管理员可以在需求详情页重新发送。
5. 幂等键在发送成功后生效，失败任务允许重试。

这部分尚未完成，不能因为当前投递路径已经修通就从复盘中删除。

## 常见错误排查

| 错误或现象 | 判断 | 处理 |
|---|---|---|
| `open_id cross app` | 使用了其他应用生成的 `open_id` | 从人员快照取邮箱，由当前机器人查询 `user_id` |
| `99991672` 且要求 `contact:user.employee_id:readonly` | 缺少返回 `user_id` 的字段权限，或权限尚未发布 | 添加权限，发布机器人应用版本并完成审批 |
| `Bot has NO availability to this user` | 用户不在机器人可用范围 | 扩大应用可用范围并重新发布 |
| 查询接口没有返回 `user_id` | 邮箱不存在、用户离职、数据范围不包含用户，或只保存了企业邮箱字段 | 核对实际 `email` 和通讯录范围 |
| 节点完成但消息失败 | 状态更新与通知不是同一事务 | 查看逐人错误，使用新需求复测，后续补充重试入口 |
| Base 写入成功但查询库字段为空 | 写入映射遗漏或字段类型不匹配 | 回读目标记录，核对字段适配层 |
| release 一直显示旧提交 | 代码没有推到远端，或发布仍在进行 | 检查远端 SHA，继续轮询到 `finished` |

## 下次开发的标准流程

### 开发前

- [ ] 明确需求单、事件、参数和归档合并的主键。
- [ ] 导出 Base 字段契约，记录真实写入样例。
- [ ] 定义人员快照结构，至少包含 `name`、`email`、来源 ID 和来源系统。
- [ ] 画出机器人身份流转，标记每个 ID 的作用域。
- [ ] 列出 API 权限、通讯录数据范围、应用可用范围和审批责任人。
- [ ] 确定通知失败是否阻塞流程，以及如何重试。

### 开发中

- [ ] Base 字段统一经过 normalize 层。
- [ ] 默认人员使用共享配置，禁止前后端重复硬编码。
- [ ] 多人字段始终按数组处理。
- [ ] 通知按人隔离结果，日志中不输出完整敏感信息。
- [ ] 页面样式用浏览器计算值和截图验证。
- [ ] 归档后回读正式库和查询库。

### 发布前

- [ ] `git status` 没有未确认的改动。
- [ ] 测试、Lint、类型检查和生产构建通过。
- [ ] 机器人权限已经添加、发布并通过审批。
- [ ] 通讯录范围和机器人可用范围覆盖测试人员。
- [ ] 目标提交已经推送到 `sprint/default`。

### 发布后

- [ ] `release-get` 返回 `status=finished`。
- [ ] `commit_id` 等于目标提交。
- [ ] 创建一张测试需求并推进一个真实节点。
- [ ] 确认机器人消息送达所有目标人员。
- [ ] 确认归档负责人、校验人和稳定归档时间已回传。
- [ ] 确认归档完成后返回工作台。

推荐执行：

```bash
npm run lint
npm test -- --runInBand
npm run build:prod
git push origin sprint/default
/Users/ray/.codex/skills/feishu-sunwen-cli/scripts/sunwen-lark-cli apps +release-create --app-id app_17apvbcusvs --branch sprint/default --as user
/Users/ray/.codex/skills/feishu-sunwen-cli/scripts/sunwen-lark-cli apps +release-get --app-id app_17apvbcusvs --release-id <release_id> --as user
```

## 后续改进

- [ ] 增加通知 Outbox、自动重试和手动补发。
- [ ] 增加管理员通知诊断页，按邮箱检查 `user_id` 查询、可用范围和消息发送。
- [ ] 为历史需求补齐人员邮箱，减少旧 `open_id` 快照造成的跳过。
- [ ] 从 Base 元数据自动生成字段契约文档。
- [ ] 建立一条测试需求专用的全流程冒烟脚本。
- [ ] 把机器人权限与发布状态纳入上线检查表，不再依赖口头确认。

## 留给后续项目的判断

1. 看到人员 ID 时，先问它属于哪个租户、哪个应用、哪个系统。
2. 看到接口成功时，继续检查目标字段是否真的写入或返回。
3. 看到测试通过时，区分代码测试和外部系统验证。
4. 看到权限已添加时，继续检查数据范围、可用范围和发布状态。
5. 看到发布任务已创建时，继续等到 `finished` 并比对提交号。

新项目启动时先复制这份清单。发布记录里保留目标 SHA、release ID、真实通知结果和 Base 回读截图，缺一项就不写 `已完成上线`。

## 参考资料

- [飞书用户身份说明](https://open.feishu.cn/document/server-docs/contact-v3/user/field-overview)
- [通过手机号或邮箱获取用户 ID](https://open.feishu.cn/document/uAjLw4CM/ukTMukTMukTM/reference/contact-v3/user/batch_get_id)
- [飞书 API 权限列表](https://open.feishu.cn/document/server-docs/application-scope/scope-list?lang=zh-CN)

返回：[[复盘库索引]]
