# 飞书 / 妙搭小程序开发经验复盘

**核心结论：这次反复出问题的根因不是某一个前端按钮或接口，而是没有在开发前把“Base 字段契约、业务粒度、人员身份、权限边界、通知可用性、发布验证”一次性定义清楚。后续做飞书小程序或妙搭应用，必须先做契约审计，再做功能开发。**

本文用于沉淀 Pollo AI 埋点项目管理系统本轮开发中的错误、修复经验和后续标准流程，避免在类似的飞书 Base / 妙搭 / Bot 项目中重复踩坑。

## 最重要的工程结论

- **Base 不是普通数据库，字段契约必须前置冻结：** 字段是否必填、字段类型、人员字段结构、附件字段、双向链接字段、日期字段都会直接决定写入是否成功，不能靠前端表单“看起来有/没有”来推断。
- **工作流系统必须先定义业务粒度：** 本项目里工作台和待办应按需求单粒度，设计按埋点事件粒度，参数按参数粒度；如果粒度混乱，状态、权限、待办、归档都会错。
- **人员身份不能只存一个 ID：** 飞书里同一个人可能有 `open_id`、`user_id`、邮箱、人员字段对象、历史数字快照等多种形态；展示、权限、通知必须分别做标准化。
- **权限配置和项目角色要分层：** 全局权限只配置管理员和普通用户；数据负责人、研发负责人、DS 验收人等应跟随具体需求单，不应作为全局固定角色硬编码。
- **通知能力有两层门槛：** 开启 Bot 权限不等于用户能收到私信；飞书开放平台应用的 Bot 可用范围必须覆盖用户或部门，并且需要重新发布应用版本。
- **妙搭发布读取远端分支：** 本地提交不等于线上发布；必须 push 后创建 release，并通过 release-get 确认 `commit_id` 和 `status=finished`。

## 本轮主要问题、根因与后续做法

| 问题域 | 典型表现 | 根因 | 后续标准做法 |
|---|---|---|---|
| Base 必填字段 | 创建需求时报 `DS验收证据`、`DS验收时间`、`稳定归档时间`、`需求链接` 必填 | 下游节点字段被设置成创建阶段必填，前端没有对应输入 | 每个节点只写当前节点字段；Base 中下游字段不得作为提需阶段必填 |
| Base 字段类型 | 日期要求数字、UI 图要求文本、双向链接要求字符串数组 | 没有按飞书字段类型做统一序列化 | 建立 Base 写入适配层，所有字段写入前统一 normalize |
| 人员展示 | 负责人显示数字 ID、未知用户，或联系人卡片 sticky | 人员字段对象、历史快照和 UI 组件展示逻辑混用 | 人员字段统一保存 `name/email/open_id/user_id`；展示只读标准化姓名 |
| 需求表单透传 | 提需时填写的负责人、需求名称没有进入需求录入环节 | 创建表单和详情表单使用了不同字段来源 | 需求单创建后立即回写需求 ID、需求名称、项目参与人，并作为详情页主数据源 |
| 工作流状态 | 已进入埋点设计但仍显示埋点提需；评审通过后仍可再次点击通过 | 状态更新按单个事件处理，需求级状态没有同步 | 所有节点推进按需求 ID 聚合更新；前端按钮依据需求级状态禁用 |
| Base 同步 | App 保存了但后台 Base 没有数据；归档事件同步了但参数没同步 | App 状态和 Base 写入不是同一事务链路 | 每次保存、节点完成、归档都明确对应 Base 写入；关键节点加回读校验 |
| 废弃数据 | 废弃事件或参数混在正式查询表、设计表中 | “删除”“作废”“正式下线”语义混淆 | 设计阶段删除就是删除草稿；正式废弃进入废弃表，不能污染正式库 |
| 需求与事件关系 | 一个需求只能维护一个 evt_id，多个事件被拆成多个需求 | 缺少稳定需求 ID，只靠需求链接或 evt_id 聚合 | 新增 `需求ID`，用需求 ID 聚合同一需求下多个事件 |
| 并行修订 | 两个需求同时修订同一个 evt_id 时互相阻塞 | 用 evt_id 当唯一进行中键 | 使用 `需求ID + evt_id` 隔离设计草稿；归档时增量合并 |
| 正式库复用 | 复用已有事件时复制全部参数，造成冗余 | 把复用理解成全量复制，而不是变更集 | 只导入本次需要修改的参数或枚举；归档 append/merge 到正式库 |
| 参数合并 | 新枚举值覆盖正式库旧枚举 | 合并逻辑缺少 append-only 约束 | 正式库归档只追加新增值，按参数 key 去重，不覆盖历史枚举 |
| 性能 | 切换事件、保存参数都重新加载整页 | Base 读取太粗，前端状态没有本地缓存 | 详情页一次加载同需求数据；保存后局部更新，必要时再回读 |
| 权限 | 研发负责人只能开发，不能看/改需求录入到设计内容 | 全局角色和项目角色边界不清 | 被需求单指定的角色拥有对应节点阅读和编辑权限 |
| 待办展示 | 待办按 evt_id 展示，用户误以为某个事件不在待办 | 待办粒度错用事件粒度 | 待办统一按需求单展示，事件数量作为摘要信息 |
| Bot 通知 | `cannot resolve openid`、多人只成功一人、Bot 不可用 | 只依赖 open_id，且混淆权限 scope 与 Bot 可用范围 | 支持 open_id/email/user_id 多通道；逐人串行发送；明确提示 Bot 可用范围问题 |

## 后续 Feishu / Base 开发前置检查清单

**核心原则：先审计 Base 和飞书能力边界，再写 UI 和业务代码。**

### Base 字段契约检查

- **字段清单：** 列出每张表、每个视图、每个字段的字段名、字段类型、是否必填、是否只读、是否公式字段。
- **写入契约：** 明确哪些字段由 App 写入，哪些字段由 Base 公式、自动化或人工维护。
- **节点契约：** 提需、设计、评审、开发、验收、上线、归档分别允许写哪些字段。
- **类型样例：** 对日期、附件、人员、单选、多选、双向链接、URL、文本字段保留真实写入样例。
- **回滚策略：** 测试数据、废弃数据、错误写入需要有单独清理脚本或废弃表承接。

### 业务粒度检查

- **需求单粒度：** 工作台、待办、权限、流程推进、通知、状态汇总都以需求 ID 为主键。
- **事件粒度：** `evt_id`、事件名、事件定义、触发时机、UI 图、变更类型按事件维护。
- **参数粒度：** 参数 key、数据类型、必传规则、枚举值、适用端、版本按参数维护。
- **合并粒度：** 归档时按 `需求ID + evt_id + 参数key` 找到本次变更，再合并到正式库。

### 人员身份检查

- **展示字段：** 前端展示永远优先使用标准化姓名，不直接展示数字 ID。
- **权限字段：** 权限判断使用标准化后的人员集合，不能混用历史快照。
- **通知字段：** 通知优先使用 Base 人员字段中的可投递身份；没有 `open_id` 时支持邮箱或 `user_id`。
- **多人场景：** 所有人员字段都按数组处理；单人只是数组长度为 1 的特例。

### 权限与可见性检查

- **妙搭可见范围：** 决定谁能打开页面。
- **App 内权限：** 决定谁能创建、查看、编辑需求。
- **需求角色权限：** 决定被指定为数据负责人、研发负责人、DS 验收人后能操作哪些节点。
- **Bot 可用范围：** 决定机器人能否私信某个用户，与页面可见范围不是一回事。

### 通知能力检查

- **应用凭证：** 环境变量 `FEISHU_APP_ID`、`FEISHU_APP_SECRET` 已配置到妙搭后台。
- **机器人能力：** 飞书开放平台已开启机器人，并完成发布。
- **权限范围：** 至少确认联系人读取、消息发送相关权限已经开通并发布。
- **用户可用范围：** Bot 应用可用范围覆盖目标用户或部门。
- **投递策略：** 多人负责人逐个发送，失败原因按人返回，不阻断已成功用户。

### 发布验证检查

- **本地状态：** `git status --short` 必须干净或只包含明确要发布的改动。
- **远端同步：** release 前必须 push 到 `sprint/default`。
- **发布确认：** release-get 必须返回 `status=finished`。
- **提交确认：** release-get 的 `commit_id` 必须等于本次要发布的提交。
- **线上冒烟：** 至少验证创建需求、保存设计、节点推进、通知发送、归档同步 5 条主链路。

## 工作流系统设计守则

**核心结论：流程动作必须和业务最小闭环保持一致；不要让底层明细表的粒度反向支配用户体验。**

- **用户看到的是需求，不是数据库行：** 工作台、待办、状态条、提交评审、节点通知都应围绕需求单。
- **设计交付需要事件明细：** 一个需求可包含多个埋点事件，研发需要快速查看每个事件和全部参数。
- **参数是事件的明细，不是流程主体：** 参数可以高频新增、删除、批量删除，但不应独立驱动需求状态。
- **同名事件允许并行修订：** 不同需求可以同时改同一个正式 `evt_id`，草稿用需求 ID 隔离。
- **归档是合并，不是覆盖：** 正式库应保留历史参数和枚举，在此基础上 append 新增内容。
- **仅校验模式不修改正式库：** 如果需求只是提醒研发校验已有埋点，不应在归档时产生正式库变更。

## 飞书 Bot 通知专项经验

**核心结论：飞书 Bot 私信的问题通常不在代码发送按钮，而在“身份解析 + 应用可用范围 + 权限发布”这三件事。**

- **不要强依赖 open_id：** Base 人员字段可能没有 `open_id`，但有邮箱或 `user_id`；代码应按多通道投递。
- **不要把 scope 报错当成唯一问题：** 如果邮箱发送已经返回 `Bot has NO availability to this user`，核心问题是 Bot 可用范围，不应再继续输出 employee_id scope 噪音。
- **多人发送必须逐个隔离：** 一个负责人失败不能让另一个负责人收不到；前端应展示“成功 1/2、失败人及原因”。
- **Bot 可用范围需要重新发布：** 在飞书开放平台加入用户或部门后，需要发布应用版本，用户才可能收到私信。
- **通知结果要进入用户反馈：** 节点完成后不能只提示“保存成功”，还要说明通知是否送达，避免流程黑盒。

## 质量门禁建议

**核心结论：飞书小程序的质量门禁要覆盖“业务主链路 + Base 写入 + 飞书能力”，只跑前端 build 不够。**

- **单元测试：** 覆盖 Base 字段 normalize、人员字段标准化、工作流状态推进、正式库归档合并、通知投递策略。
- **集成测试：** 使用真实或准真实 Base 字段结构验证创建、保存、推进、归档。
- **UI 冒烟：** 验证新增需求、多事件设计、参数批量操作、复用正式事件、研发交付视图、UI 图预览。
- **通知冒烟：** 至少测试单负责人、多人负责人、Bot 不可用、邮箱投递、open_id 投递。
- **发布冒烟：** release 后在真实线上地址完成一条从需求创建到节点推进的最小路径。

推荐发布前执行：

```bash
npm run lint
npm run test
npm run build:server
npm run build:client
git push origin sprint/default
/Users/ray/.codex/skills/feishu-sunwen-cli/scripts/sunwen-lark-cli apps +release-create --as user --app-id app_17apvbcusvs --branch sprint/default
/Users/ray/.codex/skills/feishu-sunwen-cli/scripts/sunwen-lark-cli apps +release-get --as user --app-id app_17apvbcusvs --release-id <release_id>
```

## 以后不要再犯的错误

- **不要用前端表单推断 Base 字段约束：** 必填和类型必须以 Base 实际字段为准。
- **不要用需求链接当需求主键：** 链接不稳定，必须使用自生成且不重复的需求 ID。
- **不要让 evt_id 驱动需求待办：** evt_id 是设计对象，不是待办对象。
- **不要把设计删除等同于正式作废：** 草稿删除、正式废弃、正式下线是三个不同动作。
- **不要覆盖正式库枚举：** 新增枚举必须 append 到已有枚举集合。
- **不要把人员字段当纯字符串：** 飞书人员字段是结构化对象，且可能带多个身份。
- **不要把 Bot 权限和 Bot 可用范围混为一谈：** 权限开通只能允许接口调用，可用范围才决定能否触达用户。
- **不要发布未 push 的本地提交：** 妙搭 release 读取远端分支，发布前必须确认远端 commit。

## 推荐的下一步沉淀

- **Base 契约文档自动生成：** 从常量配置或 Base 元数据生成字段契约表，减少人工维护遗漏。
- **全链路冒烟脚本：** 用测试需求自动走通创建、设计、评审、开发、验收、上线、归档。
- **通知诊断页：** 管理员可以输入邮箱，检查 open_id 解析、Bot 可用范围和私信投递结果。
- **数据治理后台：** 对状态不一致、缺需求名称、废弃数据混表、参数未归档等问题提供一键巡检。
